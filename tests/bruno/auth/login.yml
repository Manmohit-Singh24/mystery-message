info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{server}}/auth/login"
  body:
    type: json
    data: |-
      {
        "identifier" : "aa1",
        "password": "aaaaaaa1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
