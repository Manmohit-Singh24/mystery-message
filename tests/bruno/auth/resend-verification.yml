info:
  name: resend-verification
  type: http
  seq: 5

http:
  method: POST
  url: "{{server}}/auth/resend-verification"
  body:
    type: json
    data: |-
      {
          "email": "a1@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
